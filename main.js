const findQuarter = (numToQuarter) => {
    const newValue = numToQuarter / 4;
    return newValue 

}
// Invoke the function and store the return value
const returnedNum = findQuarter(8)

// Log the return value
console.log(returnedNum);